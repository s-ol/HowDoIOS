; dep.dasm: dependency interrupts
;
; This file is part of HowDoIOS.
; Visit our git repo at https://github.com/S0lll0s/HowDoIOS
;  Copyright (C) 2013 S0lll0s

; DEP_LOAD (0xf020)
; load exports of interface number A into import structure at B
:_dep_load
	SET A, PEEK
	; TODO:
	; if import == 0:
		; give hardware id
	; else if in _dependency_exports:
		; get pid
		; load exports
	; else
		; return 0
	RFI 0